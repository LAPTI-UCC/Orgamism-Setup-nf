process {
    // Default resources for all processes
    cpus = 2
    memory = 4.GB

    // Configuration for high-resource processes
    withLabel: process_high {
        cpus = 40
        memory = 20.GB
    }
}
